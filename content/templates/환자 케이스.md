<%*
// 1. 환자 정보 입력
const name = await tp.system.prompt("환자 이름을 입력하세요");
if (!name) return;

const year = await tp.system.prompt("생년월일을 입력하세요 (예: 950101)");
if (!year) return;

const date = tp.date.now("YYYY-MM-DD");
const fileBaseName = `${name}-${year}-${date}`;

// 2. 필수 폴더 자동 생성 확인
async function ensureFolder(folderPath) {
    if (!(await app.vault.adapter.exists(folderPath))) {
        await app.vault.createFolder(folderPath);
    }
}
await ensureFolder("4. 임상/임상례");
await ensureFolder("시스템/통증지도");
await ensureFolder("시스템/닥터가이던스");

// 3. 통증지도 파일 복사
const srcMap = "시스템/통증지도.md";
const destMap = `시스템/통증지도/통증지도-${fileBaseName}.md`;
const srcMapFile = app.vault.getAbstractFileByPath(srcMap);
if (srcMapFile && !(await app.vault.adapter.exists(destMap))) {
    await app.vault.copy(srcMapFile, destMap);
}

// 4. 닥터가이던스 전체 서식 파일 생성
const docPath = `시스템/닥터가이던스/닥터가이던스-${fileBaseName}.md`;
const docContent = `# 닥터가이던스-${fileBaseName}

- 환자명: ${name}
- 날짜: ${date}

### 🧠 현재 상태 요약
최근의 몸 상태는 다음과 같은 특징을 보이고 있습니다:
- 
- 

### 💊 치료 방향
- 
- 

### 🍽️ 생활관리 수칙
아래 사항을 꾸준히 실천해주시면 회복에 큰 도움이 됩니다:
- **식사:** 
- **수면:** 
- **운동/활동:** 
- **기타 습관:** 

### ⚠️ 주의사항
치료 효과를 떨어뜨리거나 증상을 악화시킬 수 있는 행동은 피해주세요:
- 
- 

### 🔄 예후 및 경과 예측
회복은 다음과 같은 단계로 진행될 수 있습니다:
- **염증기:** 증상이 다소 불편할 수 있으나 자연스러운 회복 과정입니다.
- **재생기:** 기능이 점차 회복되며 증상이 가벼워집니다.
- **회복기:** 컨디션이 안정되고 정상 생활로 복귀 가능해집니다.

### ✅ 다음 내원 시 계획
- 
`;

if (!(await app.vault.adapter.exists(docPath))) {
    await app.vault.create(docPath, docContent);
}

// 5. 현재 메인 차트 파일을 '4. 임상/임상례' 폴더로 이동 및 이름 지정
await tp.file.move(`4. 임상/임상례/${fileBaseName}`);
-%>
- **이름:** <% name %>
- **기록일:** <% date %>
- **생년월일:** <% year %>
- **연령/성별:** 
- **주호소(Main Complaint):** 
- **초진/재진 여부:** 
- [ ] 초진
- [ ] 재진

---

![[통증지도-<% fileBaseName %>]]

## 🗣️ S: Subjective (주관적 정보)
- **주요 증상:** 
- **증상 발생 시기 및 경과:** 
- **생활/직업/수면/스트레스 요인:** 
- **기타 자각 증상 / 불편감:** 
- **과거력 및 가족력:** 

## 👀 O: Objective (객관적 소견)
- **체형 및 인상 관찰:** 
- **피부/안색/눈빛/근육톤 등 외관:** 
- **복진/맥진/설진 소견:** 
- **기타 진단도구 결과:** 

## 🧠 A/P: Assessment & Plan (평가 및 치료계획)
- **추정 진단/변증:** 
- **치료 계획:** 
- **닥터가이던스 링크:** [[닥터가이던스-<% fileBaseName %>]]
