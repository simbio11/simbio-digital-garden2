<%*
const name = await tp.system.prompt("환자 이름-생년월일을 입력하세요")
const start = await tp.system.prompt("복약 시작일을 입력하세요 (YYYY-MM-DD)")
const survey = await tp.system.prompt("설문지 URL을 입력하세요 (없으면 비워두세요)")

const day7 = moment(start).add(7, "days").format("YYYY-MM-DD")
const day14 = moment(start).add(14, "days").format("YYYY-MM-DD")
const day21 = moment(start).add(21, "days").format("YYYY-MM-DD")
const day28 = moment(start).add(28, "days").format("YYYY-MM-DD")
const today = tp.date.now("YYYY-MM-DD")
const elapsed = moment().diff(start, "days")

const destFile = `시스템/복약관리/${name}-복약관리.md`
const content = `---
환자명: ${name}
복약시작일: ${start}
최근설문보낸날짜: ${start}
설문지URL: ${survey}
tags: [복약관리]
---
## 📋 복약 추적 요약

- 복약 시작: ${start}
- 오늘 날짜: ${today}
- 경과일수: ${elapsed}일

## ✅ 루틴 체크

- [ ] D+7 설문 전송 (${day7})
- [ ] D+14 내원 확인 (${day14})
- [ ] D+21 설문 전송 (${day21})
- [ ] D+28 내원 확인 (${day28})

## 🧠 메모
- 피드백:
- 계획:
`

const file = await app.vault.create(destFile, content)
tR += `✅ 복약 관리 → [[${file.path}]]
%>
